services:
  db:
    image: postgres:16
    container_name: oil_flows_db
    environment:
      POSTGRES_USER: oiluser
      POSTGRES_PASSWORD: oilpass
      POSTGRES_DB: oilflows
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
